# and

x = 5
y = 10
print(x > 4 and y < 15)  # both conditions are true, so the result is True

# or

x = 5
y = 10
print(x > 4 or y < 11)  # one condition is true, so the result is True

# not

x = 5
print(not(x > 10))  # the condition is false, so the result is True
